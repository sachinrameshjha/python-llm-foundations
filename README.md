# 🐍 Python Foundations

Built by [@sachinrameshjha](https://github.com/sachinrameshjha) as part of a Python → LLM Engineering.

---

## 📁 Notebooks

| Notebook | Contents |
|----------|----------|
| `Foundation_Boss.ipynb` | 5 synthesis challenges — no hints, no scaffolding |

---

## 🗺️ What's Covered

### Variables, Types, f-strings
- Variable assignment, reassignment, multi-assignment
- `type()`, type casting (`int()`, `str()`, `float()`)
- f-string formatting including `{expr}` and `:.2f`

### Lists, Loops, List Comprehensions
- List creation, indexing, slicing, `.append()`, `.pop()`
- `for` loop, `range()`, `enumerate()`
- List comprehensions: transform, filter, combined

### Dictionaries, Nested Dicts, `.get()`
- Dict creation, access, add/update/delete keys
- `.get()`, `.keys()`, `.values()`, `.items()`
- Nested dicts, nested access, looping with calculations

### Functions, Return, Lambda, `max(key=)`
- Defining functions, `return` values, `None`-returning functions
- Default parameters, keyword arguments
- `lambda` functions, `max()` and `min()` with `key=lambda`

### try/except, Error Types, Defensive Coding
- `try/except` basics, `as e` for error inspection
- Catching `KeyError`, `IndexError`, `TypeError` specifically
- Guard clauses, `.get()` with defaults

### Classes, `__init__`, `self.`, Methods
- Defining a class, `__init__`, instance attributes
- `self.` — the difference between instance and local variables
- Writing methods, calling methods from within other methods

---

## 🏆 Challenges

| # | Challenge | Concepts Tested |
|---|-----------|-----------------|
| 1 | Trade Logger | Functions, dicts, lists, f-strings |
| 2 | Stock Screener | List comps, lambda, max/min, `.get()` |
| 3 | Bulletproof Calculator | Default params, guard clauses, try/except |
| 4 | Nested Portfolio Report | Nested dicts, PnL formatting, list comps |
| 5 | TradingAccount Class | Full OOP — buy, sell, summary, guard clauses |

---

## 👤 About

Sachin Jha — Data Analyst with 5+ years in financial services (JPMorgan Chase, Citi, Franklin Templeton).  
Currently building toward AI/ML engineering.  
[GitHub](https://github.com/sachinrameshjha) · [LinkedIn](https://linkedin.com/in/sachinrameshjha/)
